# NajmPlay - منصة IPTV للأفلام والمسلسلات 🎬

منصة متكاملة لمشاهدة الأفلام والمسلسلات والقنوات الرياضية المباشرة مع نظام اشتراكات مدفوع وتجربة مجانية لمدة 24 ساعة.

## 🌟 المميزات الرئيسية

- ✅ **تجربة مجانية 24 ساعة** - ابدأ الاستخدام فوراً بدون الحاجة لبطاقة ائتمان
- 🎬 **مكتبة ضخمة من المحتوى** - آلاف الأفلام والمسلسلات
- 📺 **قنوات مباشرة** - قنوات رياضية وترفيهية بث مباشر
- 💳 **دفع آمن عبر PayPal** - اشتراكات تلقائية مع خيار الإلغاء في أي وقت
- 📱 **دعم WhatsApp** - تواصل مباشر وإشعارات تلقائية
- 👥 **دعم أجهزة متعددة** - شاهد على أكثر من جهاز حسب خطتك
- 🔒 **آمن ومحمي** - تشفير كامل وحماية المحتوى
- 🌐 **واجهة عربية RTL** - تجربة استخدام محسّنة للغة العربية

## 🛠 التقنيات المستخدمة

- **Frontend & Backend**: Next.js 14 (Pages Router + API Routes)
- **Database**: PostgreSQL مع Prisma ORM
- **Styling**: Tailwind CSS
- **Authentication**: JWT + Refresh Tokens
- **Payment**: PayPal Subscriptions API
- **Messaging**: WhatsApp Business API
- **Security**: bcrypt لتشفير كلمات المرور، HMAC لتوقيع URLs

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:

- **Node.js** 16.x أو 18.x أو أحدث
- **PostgreSQL** 12 أو أحدث
- **npm** أو **yarn**
- حساب **PayPal Business** (للدفع)
- **WhatsApp Business API** أو مزود وسيط (للرسائل)

## 🚀 التثبيت والتشغيل المحلي

### الخطوة 1: استنساخ المستودع

\`\`\`bash
git clone https://github.com/yaseenyaseen123/-NajmPlay.git
cd -NajmPlay
\`\`\`

### الخطوة 2: تثبيت المكتبات

\`\`\`bash
npm install
\`\`\`

### الخطوة 3: إعداد قاعدة البيانات

1. إنشاء قاعدة بيانات PostgreSQL:

\`\`\`bash
# تسجيل الدخول إلى PostgreSQL
psql -U postgres

# إنشاء قاعدة البيانات
CREATE DATABASE najmplay;

# الخروج
\\q
\`\`\`

2. نسخ ملف البيئة وتعديل المتغيرات:

\`\`\`bash
cp .env.example .env
\`\`\`

3. تحديث ملف `.env` بمعلومات قاعدة البيانات الخاصة بك:

\`\`\`env
DATABASE_URL="postgresql://user:password@localhost:5432/najmplay?schema=public"
\`\`\`

### الخطوة 4: تشغيل الترحيلات (Migrations)

\`\`\`bash
npx prisma migrate dev --name init
\`\`\`

### الخطوة 5: توليد Prisma Client

\`\`\`bash
npx prisma generate
\`\`\`

### الخطوة 6: إدخال البيانات التجريبية (Seed)

\`\`\`bash
npm run prisma:seed
\`\`\`

هذا السكربت سينشئ:
- ✅ حساب مسؤول: `admin@najmplay.test` / `P@ssw0rd123`
- ✅ حساب مستخدم تجريبي: `user@test.com` / `User123!`
- ✅ 3 أفلام تجريبية
- ✅ 1 مسلسل مع موسمين (10 حلقات)
- ✅ 2 قناة مباشرة
- ✅ خطتي اشتراك (شهرية وسنوية)

### الخطوة 7: تشغيل التطبيق

\`\`\`bash
npm run dev
\`\`\`

افتح المتصفح على: [http://localhost:3000](http://localhost:3000)

## 🔐 الحسابات الافتراضية

### حساب المسؤول (Admin)
- **البريد الإلكتروني**: `admin@najmplay.test`
- **كلمة المرور**: `P@ssw0rd123`
- **الصلاحيات**: الوصول الكامل للوحة التحكم

### حساب مستخدم تجريبي
- **البريد الإلكتروني**: `user@test.com`
- **كلمة المرور**: `User123!`
- **الصلاحيات**: مستخدم عادي مع اشتراك تجريبي

⚠️ **تحذير أمني**: يجب تغيير هذه الحسابات فوراً في بيئة الإنتاج!

## 💳 إعداد PayPal

### 1. إنشاء تطبيق PayPal

1. سجل الدخول إلى [PayPal Developer Dashboard](https://developer.paypal.com/dashboard/)
2. انتقل إلى **My Apps & Credentials**
3. قم بإنشاء تطبيق جديد (New App)
4. احصل على **Client ID** و **Secret**

### 2. تكوين Sandbox للاختبار

\`\`\`env
NEXT_PUBLIC_PAYPAL_CLIENT_ID="your-sandbox-client-id"
PAYPAL_CLIENT_SECRET="your-sandbox-secret"
\`\`\`

### 3. إنشاء خطط الاشتراك (Subscription Plans)

استخدم [PayPal Subscriptions API](https://developer.paypal.com/docs/subscriptions/) لإنشاء خطط اشتراك:

\`\`\`bash
# مثال على إنشاء خطة شهرية مع تجربة مجانية 1 يوم
curl -X POST https://api-m.sandbox.paypal.com/v1/billing/plans \\
  -H "Content-Type: application/json" \\
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \\
  -d '{
    "product_id": "PRODUCT_ID",
    "name": "Monthly Plan",
    "billing_cycles": [
      {
        "frequency": {
          "interval_unit": "DAY",
          "interval_count": 1
        },
        "tenure_type": "TRIAL",
        "sequence": 1,
        "total_cycles": 1,
        "pricing_scheme": {
          "fixed_price": {
            "value": "0",
            "currency_code": "USD"
          }
        }
      },
      {
        "frequency": {
          "interval_unit": "MONTH",
          "interval_count": 1
        },
        "tenure_type": "REGULAR",
        "sequence": 2,
        "total_cycles": 0,
        "pricing_scheme": {
          "fixed_price": {
            "value": "9.99",
            "currency_code": "USD"
          }
        }
      }
    ]
  }'
\`\`\`

### 4. إعداد Webhooks

1. في PayPal Dashboard، انتقل إلى **Webhooks**
2. أضف webhook جديد بالـ URL: `https://your-domain.com/api/paypal/webhook`
3. اختر الأحداث التالية:
   - `BILLING.SUBSCRIPTION.CREATED`
   - `BILLING.SUBSCRIPTION.ACTIVATED`
   - `PAYMENT.SALE.COMPLETED`
   - `BILLING.SUBSCRIPTION.CANCELLED`
   - `BILLING.SUBSCRIPTION.SUSPENDED`
   - `BILLING.SUBSCRIPTION.EXPIRED`
   - `PAYMENT.SALE.REFUNDED`

4. احصل على **Webhook ID** وأضفه إلى `.env`:

\`\`\`env
PAYPAL_WEBHOOK_ID="your-webhook-id"
\`\`\`

⚠️ **هام**: ملف `/pages/api/paypal/webhook.js` يحتوي على TODO للتحقق من توقيع webhook. يجب تنفيذه للأمان!

## 📱 إعداد WhatsApp Business API

### الخيار 1: استخدام Twilio

1. إنشاء حساب [Twilio](https://www.twilio.com/)
2. تفعيل WhatsApp Business API
3. الحصول على Credentials:

\`\`\`env
WHATSAPP_API_URL="https://api.twilio.com/2010-04-01/Accounts/YOUR_ACCOUNT_SID/Messages.json"
WHATSAPP_API_KEY="YOUR_AUTH_TOKEN"
\`\`\`

### الخيار 2: استخدام مزود آخر

يمكنك استخدام أي مزود WhatsApp Business API مثل:
- MessageBird
- 360Dialog
- Vonage
- GreenAPI

قم بتحديث `WHATSAPP_API_URL` و `WHATSAPP_API_KEY` في `.env` حسب المزود.

### قوالب الرسائل المتاحة

الملف `pages/api/whatsapp/send.js` يحتوي على قوالب رسائل جاهزة:
- `WELCOME` - رسالة ترحيب للمستخدمين الجدد
- `OTP` - رمز التحقق
- `TRIAL_ENDING` - تذكير بانتهاء التجربة
- `TRIAL_ENDED` - إشعار بانتهاء التجربة
- `SUBSCRIPTION_ACTIVATED` - تأكيد تفعيل الاشتراك
- `PAYMENT_SUCCESS` - تأكيد الدفع
- `PAYMENT_FAILED` - فشل الدفع
- `SUBSCRIPTION_RENEWED` - تجديد الاشتراك

## 🔒 الأمان والحماية

### 1. تغيير JWT_SECRET فوراً

\`\`\`env
# توليد مفتاح عشوائي قوي
JWT_SECRET="$(openssl rand -base64 32)"
\`\`\`

أو في Node.js:

\`\`\`javascript
require('crypto').randomBytes(32).toString('base64')
\`\`\`

### 2. تفعيل HTTPS في الإنتاج

**لا تستخدم HTTP في الإنتاج أبداً!** استخدم HTTPS فقط لحماية البيانات الحساسة.

### 3. التحقق من PayPal Webhooks

في ملف `/pages/api/paypal/webhook.js`، يجب تفعيل التحقق من التوقيع:

\`\`\`javascript
// TODO: استبدل هذا
async function verifyPayPalWebhookSignature(req) {
  // استخدم PayPal SDK للتحقق
  const isValid = await paypal.notification.webhookEvent.verify(
    req.headers,
    req.body,
    process.env.PAYPAL_WEBHOOK_ID
  )
  return isValid
}
\`\`\`

### 4. حماية Player Endpoints

الملف `/pages/api/player/token.js` يستخدم HMAC لتوقيع URLs. تأكد من:
- استخدام JWT_SECRET قوي
- تحديد مدة صلاحية مناسبة للـ tokens
- التحقق من الاشتراك في كل طلب

### 5. منع إساءة الاستخدام

**توصيات لمنع التجارب المتكررة:**

1. **تقييد حسب البريد الإلكتروني**: منع نفس البريد من التسجيل مرتين
2. **تقييد حسب رقم الهاتف**: ربط التجربة برقم واحد
3. **تقييد حسب IP**: تتبع عناوين IP
4. **OTP عبر WhatsApp**: طلب تحقق عبر رقم الهاتف
5. **تحليل السلوك**: اكتشاف الأنماط المشبوهة

مثال على إضافة OTP:

\`\`\`javascript
// في register endpoint
const otp = Math.floor(100000 + Math.random() * 900000)
await sendWhatsAppNotification(phone, 'OTP', otp)
// حفظ OTP في قاعدة البيانات للتحقق
\`\`\`

### 6. عدم تخزين بيانات البطاقات

**لا تخزن أبداً:**
- أرقام بطاقات الائتمان
- CVV
- تواريخ انتهاء البطاقات

PayPal يتولى معالجة المدفوعات بشكل آمن.

## 📂 هيكل المشروع

\`\`\`
-NajmPlay/
├── pages/
│   ├── _app.js                 # Next.js App wrapper
│   ├── _document.js            # Next.js Document (RTL)
│   ├── index.js                # الصفحة الرئيسية
│   ├── admin/
│   │   └── index.js            # لوحة تحكم المسؤول
│   └── api/
│       ├── auth/
│       │   ├── register.js     # تسجيل حساب جديد
│       │   └── login.js        # تسجيل الدخول
│       ├── paypal/
│       │   └── webhook.js      # معالج webhooks من PayPal
│       ├── whatsapp/
│       │   └── send.js         # إرسال رسائل WhatsApp
│       └── player/
│           └── token.js        # توليد tokens للمشغل
├── lib/
│   ├── prisma.js               # Prisma Client Singleton
│   └── auth.js                 # JWT utilities & middleware
├── prisma/
│   ├── schema.prisma           # Prisma Schema
│   └── seed.js                 # بيانات تجريبية
├── styles/
│   └── globals.css             # Tailwind + RTL styles
├── .env.example                # مثال لمتغيرات البيئة
├── .gitignore
├── package.json
├── next.config.js
├── tailwind.config.js
├── postcss.config.js
└── README.md
\`\`\`

## 🗄️ قاعدة البيانات (Prisma Schema)

النماذج الرئيسية:

- **User** - المستخدمين (Admin / User)
- **Plan** - خطط الاشتراك
- **Subscription** - الاشتراكات النشطة
- **Content** - المحتوى (أفلام / مسلسلات)
- **Series** - بيانات المسلسلات
- **Season** - المواسم
- **Episode** - الحلقات
- **Channel** - القنوات المباشرة
- **Device** - أجهزة المستخدمين
- **Payment** - المعاملات المالية
- **AuditLog** - سجل الأنشطة

## 🔧 أوامر مفيدة

\`\`\`bash
# تشغيل في وضع التطوير
npm run dev

# بناء للإنتاج
npm run build

# تشغيل في الإنتاج
npm start

# تشغيل Prisma Studio (واجهة لإدارة قاعدة البيانات)
npm run prisma:studio

# إنشاء migration جديد
npm run prisma:migrate

# إعادة توليد Prisma Client
npm run prisma:generate

# إعادة إدخال البيانات
npm run prisma:seed
\`\`\`

## 📡 API Endpoints

### Authentication

- `POST /api/auth/register` - تسجيل حساب جديد
- `POST /api/auth/login` - تسجيل الدخول

### PayPal

- `POST /api/paypal/webhook` - استقبال webhooks من PayPal

### WhatsApp

- `POST /api/whatsapp/send` - إرسال رسالة WhatsApp (Admin only)

### Player

- `GET /api/player/token?content_id=xxx` - الحصول على token لمشاهدة فيلم
- `GET /api/player/token?episode_id=xxx` - الحصول على token لمشاهدة حلقة
- `GET /api/player/token?channel_id=xxx` - الحصول على token لمشاهدة قناة

جميع endpoints محمية بـ JWT Authentication (ماعدا register و login).

## 🎨 التخصيص

### تغيير الألوان

عدّل ملف `tailwind.config.js`:

\`\`\`javascript
theme: {
  extend: {
    colors: {
      primary: '#your-color',
      secondary: '#your-color',
    }
  }
}
\`\`\`

### إضافة صفحات جديدة

أضف ملفات في مجلد `pages/` وستكون متاحة تلقائياً.

### إضافة API endpoints جديدة

أضف ملفات في `pages/api/` وستكون متاحة فوراً.

## 🚀 النشر (Deployment)

### Vercel (موصى به)

\`\`\`bash
# تثبيت Vercel CLI
npm i -g vercel

# نشر
vercel
\`\`\`

تأكد من إضافة متغيرات البيئة في لوحة تحكم Vercel.

### Docker (اختياري)

يمكنك إضافة `Dockerfile` و `docker-compose.yml` للنشر باستخدام Docker.

## ⚠️ تحذيرات مهمة

1. **لا تنشر .env على GitHub أبداً**
2. **غيّر JWT_SECRET فوراً في الإنتاج**
3. **استخدم HTTPS فقط في الإنتاج**
4. **فعّل التحقق من PayPal webhooks**
5. **غيّر حساب المسؤول الافتراضي**
6. **استخدم كلمات مرور قوية**
7. **قم بنسخ احتياطي لقاعدة البيانات بانتظام**

## 📝 TODO (للتطوير المستقبلي)

- [ ] إضافة مشغل فيديو متقدم (Video.js / Plyr)
- [ ] تحسين لوحة تحكم Admin بنماذج CRUD كاملة
- [ ] إضافة نظام تقييمات ومراجعات
- [ ] تطبيق موبايل (React Native)
- [ ] نظام توصيات ذكي
- [ ] دعم تنزيل للمشاهدة دون اتصال
- [ ] تكامل مع CDN لتوزيع المحتوى
- [ ] إحصائيات وتحليلات متقدمة
- [ ] دعم لغات متعددة
- [ ] نظام إشعارات Push

## 🤝 المساهمة

المشروع خاص حالياً. للمساهمة، يرجى التواصل مع المالك.

## 📄 الترخيص

جميع الحقوق محفوظة © 2025 NajmPlay

## 💬 الدعم

للحصول على الدعم:
- افتح Issue على GitHub
- تواصل عبر WhatsApp (سيتم إضافة الرقم)
- البريد الإلكتروني: support@najmplay.com (مثال)

---

تم بناء المشروع بـ ❤️ باستخدام Next.js و Prisma
